import 'package:flutter/material.dart';

class Categorie {
  final id;
  final title;
  final colors;
  const Categorie(
      {@required this.id,
      @required this.title,
      @required this.colors = Colors.orange});
}
